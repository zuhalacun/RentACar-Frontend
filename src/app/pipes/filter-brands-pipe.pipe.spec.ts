import { FilterBrandsPipePipe } from './filter-brands-pipe.pipe';

describe('FilterBrandsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBrandsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
